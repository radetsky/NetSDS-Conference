cd NetSDS-Conference 
make depends-ubuntu
make install 
make gui-install

# psql -U postgres -i sql/pgsql.sql 
# psql -U postgres -i sql/schema.sql 
It does create new database called 'astconf' with user 'astconf'. 

// Configure the apache2 to run .pl as .cgi 
a2enmod proxy proxy_http
configure default.conf like this: 
..

Install asterisk-configs, utils, etc/netstyle/conference.conf 

